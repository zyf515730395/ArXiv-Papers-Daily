# Collect candidates, publish the default Summary state, and deploy every day.

name: Run Arxiv Papers Daily

on:
  workflow_dispatch:
  schedule:
    # Run once daily at 01:00 UTC (09:00 Asia/Shanghai).
    - cron: "0 1 * * *"

permissions:
  contents: write

concurrency:
  group: arxiv-paper-collection
  cancel-in-progress: false

env:
  GITHUB_USER_NAME: zyf515730395
  GITHUB_USER_EMAIL: zhangyufan.aiesec@gmail.com

jobs:
  collect:
    name: collect-and-publish-candidates
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Collect candidates and generate the site
        run: python cloud_arxiv.py

      - name: Commit candidate pages
        run: |
          git config user.name "$GITHUB_USER_NAME"
          git config user.email "$GITHUB_USER_EMAIL"
          git add data/arxiv-candidates.json
          git add docs/arxiv-papers-daily-web.json docs/index.html
          if git diff --cached --quiet; then
            echo "No candidate updates"
          else
            git commit -m "Github Action Collect Arxiv Candidates"
            git push origin main
          fi
